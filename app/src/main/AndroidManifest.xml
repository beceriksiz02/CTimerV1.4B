<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Needed to keep timing reliable while backgrounded (screen lock, app switch, Doze) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Android 13+ requires this to post the persistent timer notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Restore an active timer's state after the device reboots -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Lets us politely ask the user to exempt the app from Doze/App Standby so the
         foreground service + notification aren't throttled during long focus sessions.
         We only prompt; we never silently bypass battery optimization. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.PremiumTimer"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PremiumTimer"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Keeps the active countdown/stopwatch alive and accurate while backgrounded.
             foregroundServiceType "specialUse" is the correct category for a user-visible
             timer/stopwatch that isn't media, location, or camera. -->
        <service
            android:name=".timing.TimerForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Active countdown timer or stopwatch tracking" />
        </service>

        <!-- Restores any timer/stopwatch that was still running when the device rebooted -->
        <receiver
            android:name=".timing.BootRestoreReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Handles Pause / Resume / Stop / Add Time taps from the notification -->
        <receiver
            android:name=".timing.NotificationActionReceiver"
            android:exported="false" />

        <!-- Serves the CSV export file to the system share sheet without exposing raw file:// paths -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
